<!DOCTYPE html>
<html>

<head>
  <title>
    <%= title %>
  </title>
  <link rel='stylesheet' href='/stylesheets/style.css' />
  <style>
    body {
      font-family: Arial, sans-serif;
      background-color: #121212;
      color: #ffffff;
      margin: 0;
      padding: 0;
      display: flex;
      flex-direction: column;
      align-items: center;
      justify-content: center;
      height: 100vh;
    }

    h1 {
      color: #4caf50;
    }

    p {
      margin: 20px 0;
    }

    .nav-button {
      background-color: #4caf50;
      color: white;
      border: none;
      padding: 10px 20px;
      font-size: 1rem;
      border-radius: 5px;
      cursor: pointer;
      text-decoration: none;
      transition: background-color 0.3s ease, transform 0.2s ease;
      margin: 10px;
    }

    .nav-button:hover {
      background-color: #45a049;
      transform: scale(1.05);
    }

    .home-icon {
      position: absolute;
      top: 20px;
      left: 20px;
      font-size: 2rem;
      color: #4caf50;
      cursor: pointer;
      text-decoration: none;
    }
  </style>
</head>

<body>
  <a href="/" class="home-icon">🏠</a>
  <h1>Welcome to Progress Tracker</h1>
  <p>Track your progress with simplicity and aesthetics.</p>
  <a href="/auth/login" class="nav-button">Login</a>
  <a href="/auth/signup" class="nav-button">Register</a>
  <a href="/progress/add" class="nav-button">Add Progress</a>
  <% if (userRole==="Admin" ) { %>
    <a href="/admin/dashboard" class="nav-button">Admin Dashboard</a>
    <% } %>
</body>

</html>